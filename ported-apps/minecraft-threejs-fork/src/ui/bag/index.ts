// TSYNE: Original used DOM (document.createElement, Vite .png imports, classList).
// Stubbed — bag selection is handled via keyboard events in control/index.ts.

/* Original code preserved for reference — see git history */

export default class Bag {
  current = 0
}
