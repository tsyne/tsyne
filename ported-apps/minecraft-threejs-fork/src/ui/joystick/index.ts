// TSYNE: Original used DOM (document.querySelector, pointer events, raw HTML import).
// Stubbed — no mobile/touch support in tsyne.

/* Original code preserved for reference — see git history */

export default class Joystick {
  constructor(_control: any) {}
  init = () => {}
}
