{
  "name": "tsyne-ported-apps",
  "version": "0.1.0",
  "description": "Ported applications demonstrating Tsyne features",
  "private": true,
  "scripts": {
    "test": "npm run test:logic && npm run test:gui",
    "test:logic": "jest --testMatch='**/*.logic.test.ts' --testPathIgnorePatterns='/node_modules/' --passWithNoTests",
    "test:gui": "jest --maxWorkers=1",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.0.0"
  }
}
