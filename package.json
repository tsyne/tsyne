{
  "name": "tsyne-monorepo",
  "version": "0.1.0",
  "description": "Tsyne monorepo - TypeScript wrapper for Fyne",
  "private": true,
  "scripts": {
    "build": "npm run build:bridge && npm run build:core",
    "build:bridge": "cd bridge && CGO_ENABLED=1 /usr/local/go/bin/go build -o ../bin/tsyne-bridge .",
    "build:core": "cd core && npm run build",
    "build:designer": "cd designer && npm run build",
    "build:all": "npm run build && npm run build:designer",
    "install:all": "npm install && cd core && npm install && cd ../designer && npm install && cd ../examples && npm install",
    "test": "npm run test:core",
    "test:core": "cd core && npm test",
    "test:designer": "cd designer && npm test",
    "test:examples": "cd examples && npm test",
    "test:all": "npm run test:core && npm run test:designer && npm run test:examples",
    "clean": "rm -rf node_modules && cd core && rm -rf node_modules dist && cd ../designer && rm -rf node_modules dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
