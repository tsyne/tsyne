# PhoneTop permissions - install to /etc/doas.d/20-phonetop.conf
# (NOT /etc/doas.conf - postmarketOS uses doas.d/ overrides)
permit nopass paul cmd reboot
permit nopass paul cmd poweroff
permit nopass paul cmd ip
permit nopass paul cmd sh
