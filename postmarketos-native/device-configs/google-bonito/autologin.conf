# OpenRC autologin for tty1 (postmarketOS uses BusyBox getty, not agetty)
#
# To apply: modify /etc/inittab tty1 line to:
#   tty1::respawn:/sbin/getty -n -l /bin/autologin 38400 tty1
#
# And create /bin/autologin:
#   #!/bin/sh
#   exec /bin/login -f paul
#   chmod +x /bin/autologin
