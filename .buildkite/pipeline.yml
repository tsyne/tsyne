steps:
  - label: ":golang: :nodejs: Build and Test"
    command: "./ci.sh"
    agents:
      queue: "default-queue"
    timeout_in_minutes: 20
    artifact_paths:
      - "bin/tsyne-bridge"
      - "dist/**/*"
